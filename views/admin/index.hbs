{{> admin/header title='Admin dashboard' subtitle='Admin section' }}

<main>
	<h2>
		Dashboard
	</h2>
	{{#if debateList}}
	{{#each debateList}}
	<h3>{{ debateTypeName }}</h3>
	<table class="list-debates">
		{{#if debates}}
		{{#each debates}}
		<tr>
			<td>
				<a href="/{{ ../debateTypeName }}/{{ this }}">{{ this }}</a>
			</td>
			<td>
				<a href="/{{ ../debateTypeName }}/{{ this }}">view</a>
			</td>
			<td>
				<a href="/admin/edit_debate/{{ this }}">edit</a>
			</td>
		</tr>
		{{/each}}
		{{/if}}
	</table>
	{{/each}}
	{{/if}}
</main>

{{> admin/footer}}